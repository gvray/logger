---
title: Playground
---

# Playground

在左侧编辑代码，点击 **Run** 执行。`Logger`、`LogLevel` 以及所有内置中间件都已注入为全局变量，无需手动 import——但保留 `import` 语句也可以（会被自动剥离）。

::: tip 修改源码即时生效
本页面从 `src/` 直接导入库代码。修改 `src/Logger.ts` 等源文件后，刷新页面即可看到改动效果。
:::

<Playground />
